# ExpoTest

Builder's managed Expo test app: a blank TypeScript Expo project with no committed `ios/` directory.
It exists to verify signed archive builds through the Builder CLI on GitHub Actions and TestFlight.
